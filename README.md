# Running the Application
## Prerequisites
- Docker must be installed and running

## Steps
1. Navigate to the SAGA, TCC or 2PC directory.
2. Build the application:
  ``` bash
  ./build.sh
  ```
3. Once the build completes, start the application using Docker:
  ``` bash
  docker compose up
  ```
